# VIIPER V4 Dependencies

# Core
python = "^3.10"
pydantic = "^2.5.0"

# Database
sqlalchemy = "^2.0.0"
alembic = "^1.13.0"

# CLI
typer = "^0.9.0"
rich = "^13.7.0"

# HTTP & APIs
httpx = "^0.26.0"

# Templating
jinja2 = "^3.1.0"

# NEW: Browser automation for Browse → Idea → Code pipeline
playwright = "^1.40.0"

# Async support
asyncio = "*"

# Testing
pytest = "^7.4.0"
pytest-asyncio = "^0.21.0"
pytest-cov = "^4.1.0"

# Development
black = "^23.0.0"
ruff = "^0.1.0"
mypy = "^1.7.0"